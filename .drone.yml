pipeline:
  build:
    image: node
    commands:
      - npm install ./frontend
      - npm test ./frontend
  publish:
    image: plugins/docker
    repo: dstrebel/node-drone
    secrets: [ docker_username, docker_password ]